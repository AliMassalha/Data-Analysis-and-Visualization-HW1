To run code add to the directory:

cohere_API_key.txt that contains your cohere model api key

pinecone_API_key.txt that contains your pinecone index api key

(I added the files just insert the keys)
